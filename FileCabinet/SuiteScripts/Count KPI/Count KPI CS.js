/**
 *
 * @copyright Alex S. Ducken 2020 HydraMaster LLC
 *
 * @NApiVersion 2.x
 * @NScriptType ClientScript
 * @NModuleScope SameAccount
 */
define(['N/search', 'N/ui/message', './Count KPI Fields.js'],
/**
 * @param{search} search
 * @param{message} message
 * @param{countFields} field library for Count KPI
 */
function(search, message, countFields) {
    

    function pageInit(scriptContext) {
    }

    /**
     * Function for managing the count kpi execution
     */
    function fireKPI(){

    }

    return {
        pageInit: pageInit
    };
    
});
